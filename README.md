# automated-trainings
Image Classification, Object Detection based automated training code-pieces. 

#### 1. Keras - TF-1.x : - Update & go through the scripts inside scripts and run the docker file post creating the docker image 

#### 2. Keras - TF-2.x : - Update & go through the scripts inside scripts and run the docker file post creating the docker image

#### 3. PyTorch : - Update & go through the scripts inside imagenet and run the docker file post creating the docker image

#### 4. Yolo : - Update & go through the scripts inside src folder and run the docker file post creating the docker image